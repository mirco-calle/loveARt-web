services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: production
    container_name: loveart_backend
    ports:
      - "8002:8000"
    env_file:
      - .env
    volumes:
      - ./backend/uploads:/app/uploads
    restart: always
